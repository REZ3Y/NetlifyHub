/**
 * Netlify REST client (`https://api.netlify.com/api/v1`).
 * Use after loading a per-account token from the vault (Phase 2+).
 *
 * @see repo `Netlify API _ Netlify Docs.mhtml` and https://open-api.netlify.com/
 */
export * from '@netlifyhub/netlify-client';
